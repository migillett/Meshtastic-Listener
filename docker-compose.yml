version: '3.8'
services:
  meshtastic_listener:
    build: 
      context: .
      dockerfile: dockerfile
    container_name: meshtastic_listener
    volumes:
      - ./data:/home/meshtastic/data
    environment:
      - DEVICE_INTERFACE=192.168.3.185
      - DB_NAME=listener.db
    restart: unless-stopped